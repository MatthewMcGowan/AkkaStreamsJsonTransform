kafka {
  in {
    bootstrapServers = "localhost:6001"
    groupId = "testGroup"
    topic = "testIn"
    offsetCommitMaxBatchSize = 20
  }
  out {
    bootstrapServers = "localhost:6001"
    topic = "testOut"
  }
}
